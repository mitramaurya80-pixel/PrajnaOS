== PRAJNA OS CONTEXT ==
Project: PrajnaOS — bare metal OS in C + x86 Assembly
Developer: Ravi, 2nd year CSE student, Fedora Linux
Path: /home/vega/PrajnaOS/
Editor: VS Code
Build: QEMU qemu-system-i386

== SYSTEM SPECS ==
OS: Fedora Linux, zsh shell, 16GB RAM
Compiler: gcc -m32 -ffreestanding -fno-stack-protector
Assembler: nasm -f elf32
No stdlib — custom types.h instead of stdint.h

== COMPLETED LEVELS ==
L1: Bootloader + VGA — COMPLETE
L2: GDT + IDT + keyboard — COMPLETE
L3: Shell + PIT + colors + cursor — COMPLETE
L4: ATA driver — WORKING
  - ata_init() working
  - ata_read_sector() working (reads sector 0 successfully)
  - ata_write_sector() written (not fully tested)
  - disk.img: 64MB FAT32 formatted, currently empty
  - QEMU: qemu-system-i386 -drive file=disk.img,format=raw,if=ide,bus=0,unit=0 -m 512 -boot c

== CURRENT FILES ==
kernel/boot.asm     — multiboot bootloader
kernel/kernel.c     — kernel_main()
kernel/gdt.c + gdt.asm  — GDT setup
kernel/idt.c + idt.asm  — IDT setup
kernel/isr.c        — interrupt handlers
kernel/shell.c      — basic shell
kernel/pit.c        — timer
kernel/ata.c        — ATA driver (L4)
kernel/ml/ml_math.c — ML math (started)
includes: types.h, gdt.h, idt.h, shell.h, pit.h, ata.h

== CURRENT kernel_main() ==
void kernel_main() {
    cli
    disable cursor
    print welcome message
    gdt_init()
    idt_init()
    pit_init(100)
    ata_init()
    print ATA initialized
    ata_read_sector(0, buf) — working
    print PrajnaOS>
    while(1) {}
}

== WHAT WORKS ==
- Boots from disk.img
- VGA text output
- Keyboard input via interrupts
- Shell with basic commands
- ATA read sector 0 successful
- FAT32 disk exists but no file parsing yet

== NEXT LEVELS PLANNED ==
L5: Physical memory manager (pmm.c) — bitmap allocator
L6: Multitasking — basic round-robin scheduler
L7: FAT32 file reading — parse boot sector, read files
L8: ML inference — Iris classifier in kernel space
    (research gap: ML in kernel space without stdlib)
    Target: IEEE ICACCI paper

== MINI PROJECT GOAL ==
Title: Neural Network Inference Engine in Bare Metal OS
Phase 1: Math library (no stdlib) — started in ml_math.c
Phase 2: Weight loader from FAT32 disk
Phase 3: Inference engine (forward pass)
Phase 4: Scheduler integration

== PREFERENCES ==
- Line by line comments in ALL code
- No stdlib anywhere in OS code
- Build step by step
- Explain simply
- Use types.h not stdint.h

== START HERE ==
I want to continue from L4. 
Next task: implement L5 physical memory manager.
Start by asking me to paste my Makefile and linker.ld
so you can give exact lines to add.